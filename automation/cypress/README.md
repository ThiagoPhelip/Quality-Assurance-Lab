# Cypress E2E

Testes de interface independentes para os fluxos críticos da QAZANDO Shop.

```bash
npm ci
npm test
```

Use `BASE_URL=https://outro-ambiente.example` para trocar o ambiente. Os testes evitam contas compartilhadas e não concluem compras reais.

